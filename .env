--- Agent Nexus Environment Configuration ---

1. CORE LLM API KEY (REQUIRED for application launch)

This key is used by common/llm/gemini.py.

NOTE: This key (sk-or-v1...) appears to be an OpenRouter key. The current 'google-genai'

SDK integration expects a native Gemini API key (AIza...). We use this value now

to pass the startup check, but future API calls might fail until the client is adjusted

for OpenRouter (e.g., setting base_url).

GEMINI_API_KEY="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"

2. REDIS (Task Queue / Cache)

This DSN is used by common/messaging/dramatiq.py

REDIS_DSN="redis://redis:6379/0"

3. POSTGRES (Relational Database)

These variables are used by common/config.py for database connection string generation.

POSTGRES_USER="postgres"
POSTGRES_PASSWORD="postgres"
POSTGRES_DB="agent_nexus_db"
POSTGRES_HOST="db"
POSTGRES_PORT=5432